[project]
name = "fly-in"
version = "0.1.0"
description = "A programs that handle many drones"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pygame>=2.6.1",
]

[tool.ruff]
line-length = 79
